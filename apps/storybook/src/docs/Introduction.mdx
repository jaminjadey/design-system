import { Meta } from '@storybook/addon-docs/blocks';
import { metadata } from '@demo-ds/tokens';

<Meta title="Docs/Introduction" />

# Demo Design System

This Storybook documents a demo design-system pipeline. It shows how
Figma-style token JSON becomes canonical tokens, generated CSS variables,
generated TypeScript exports, an internal Mantine theme adapter, and public
React components.

The token values and example content are generic demo data. The point is the
package workflow: one token contract feeds the theme, components, docs, and app.

## Package Flow

- `@demo-ds/tokens` exposes generated token artifacts.
- `@demo-ds/mantine-theme` keeps Mantine setup behind `DemoThemeProvider`.
- `@demo-ds/components` exports the public design-system component API.
- Storybook consumes those public package exports.

## Token Package

The current generated token package is <code>{metadata.name}</code> with
<code>{metadata.tokenCount}</code> generated tokens.

The generated source label is <code>{metadata.source}</code>.

## Local Commands

```sh
pnpm tokens:scan
pnpm --filter @demo-ds/tokens build
pnpm storybook
pnpm --filter @demo-ds/storybook build
```
