import { Meta, Source } from "@storybook/addon-docs/blocks";
import {
  Button,
  AlertBanner,
  Card,
  DatePicker,
  LoadingSpinner,
  NotificationBanner,
  PageHeader,
  SegmentedControl,
  Select,
  StatusBadge,
  TextInput,
  ThemeToggle,
  Tooltip
} from "@demo-ds/components";
import { appSetupUsage, componentUsage } from "./componentUsageSnippets";

<Meta title="Docs/Components" />

# Components

The component package is implemented with Mantine internally and uses the
generated theme and token CSS variables supplied by `DemoThemeProvider`.

Apps consume `@demo-ds/components`, not Mantine components or Mantine prop
types. That keeps the public API tied to design-system decisions instead of the
underlying rendering library.

Component-specific values such as Button tone/emphasis colours, StatusBadge
states, NotificationBanner tones, Select input states, SegmentedControl states,
DatePicker states, LoadingSpinner colours, Tooltip overlays, Card surface values,
and TextInput states come from generated `component.*` tokens exposed as
`--ds-component-*` variables.

## Initial Components

- `Button`: token-backed tone and emphasis mapping.
- `TextInput`: standard description and error behaviour.
- `AlertBanner`: accessible status and alert messaging.
- `NotificationBanner`: notification-specific tones and accessible roles.
- `Select`: token-backed form select API.
- `SegmentedControl`: token-backed active/inactive mode selection.
- `Tooltip`: token-backed overlay styling.
- `DatePicker`: date-field API with date-picker state tokens.
- `LoadingSpinner`: token-backed accessible loading status.
- `Card`: token-backed surface, radius, border, and shadow styling.
- `StatusBadge`: compact semantic state display.
- `PageHeader`: page-level title, description, and actions.
- `ThemeToggle`: design-system colour-scheme integration.

## Demo

<div className="component-demo-stack">
  <PageHeader
    title="Project overview"
    description="Generic page content using package exports."
    actions={<Button>New item</Button>}
  />
  <Card>
    <TextInput
      label="Project name"
      helperText="Use a generic demo value."
      defaultValue="Demo project"
    />
    <div style={{ height: "var(--ds-space-md)" }} />
    <Select
      label="Status"
      data={[
        { value: "draft", label: "Draft" },
        { value: "review", label: "In review" },
        { value: "live", label: "Live" }
      ]}
      defaultValue="review"
    />
  </Card>
  <AlertBanner tone="success" title="Saved">
    The demo configuration was saved.
  </AlertBanner>
  <NotificationBanner tone="info" title="Import report">
    Synthetic component tokens generated successfully.
  </NotificationBanner>
  <SegmentedControl
    label="View"
    data={[
      { value: "overview", label: "Overview" },
      { value: "activity", label: "Activity" },
      { value: "settings", label: "Settings" }
    ]}
    defaultValue="overview"
  />
  <DatePicker label="Target date" defaultValue="2026-07-13" />
  <div className="component-demo-row">
    <StatusBadge tone="neutral">Draft</StatusBadge>
    <StatusBadge tone="success">Live</StatusBadge>
    <StatusBadge tone="warning">Needs review</StatusBadge>
  </div>
  <div className="component-demo-row">
    <Tooltip label="Create a generic item" opened>
      <Button>New item</Button>
    </Tooltip>
    <LoadingSpinner label="Loading report" />
  </div>
  <ThemeToggle />
</div>

## Usage Code

The app sets up the design-system provider and stylesheet once.

<Source code={appSetupUsage} language="tsx" />

Component code imports from `@demo-ds/components`; Mantine stays behind the
design-system package boundary.

<Source code={componentUsage} language="tsx" />
