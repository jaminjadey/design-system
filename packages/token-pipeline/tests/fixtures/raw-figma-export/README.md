# Synthetic Raw Token Export

These files are invented, public demo inputs used to test the raw-token importer.
They mimic common nested Figma-token export shapes but do not contain private
brand names, private values, private assets, source IDs, or source-tool metadata.

